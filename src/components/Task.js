import React from 'react';

class Task extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div>
                {/* Maybe do it this way, start by doing it only in a list... */}
            </div>
        );
    }
}

export default Task;